# Milestones Index (Root-Level)

This index points to the three root-level milestone folders:

- `milestone3/` — first-subject analysis milestone (single-subject showcase moved to `milestone2/continuous_data_of_one_subject/`)
- `milestone4/` — all-subject processing and outlier/problem tracking (includes `raw_bids/` with `sub-*` folders)
- `milestone5/` — consolidated final reporting + reproducibility (includes `final_pipeline/`, `reports/`, `debug_logs/`)

## Direct files
- `milestone3/README.md`
- `milestone4/README.md`
- `milestone5/README.md`
